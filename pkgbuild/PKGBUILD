pkgname=dispose
pkgver=r4.f763e5c
pkgrel=1
pkgdesc="dispose folder"
arch=('any')
#depends=('perl')
source=("git+https://github.com/Vampire-Offical/dispose.git")
md5sums=('SKIP')

pkgver() {
  cd "$srcdir/dispose"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}



package() {
  mkdir -p ${pkgdir}/etc/system/systemd
  mkdir -p ${pkgdir}/lib
  cp ${srcdir}/dispose/dispose.sh ${pkgdir}/bin/dispose.sh
  cp ${srcdir}/dispose/dispose.service ${pkgdir}/etc/system/systemd
}
